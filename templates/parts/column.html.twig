<div class="col-md-4 order-md-2 mb-4">
    <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Informations</span>
    </h4>
    <ul class="list-group mb-3">
        {% set booking = app.session.get('booking') %}
        {% if booking %}
            {% for number in 1..booking.number %}
                <li class="list-group-item d-flex justify-content-between lh-condensed ticket-{{ number }}">
                    <div>
                        <h6 class="my-0">Billet Visiteur {{ number }}</h6>
                        <small class="text-muted"></small>
                    </div>
                    <span class="text-muted">0€</span>
                </li>
            {% endfor %}
        {% endif %}
        {#<li class="list-group-item d-flex justify-content-between lh-condensed">#}
            {#<div>#}
                {#<h6 class="my-0">Billet 18-25 ans</h6>#}
                {#<small class="text-muted">Billet 18-25 ans</small>#}
            {#</div>#}
            {#<span class="text-muted">25€</span>#}
        {#</li>#}
        {#<li class="list-group-item d-flex justify-content-between lh-condensed">#}
            {#<div>#}
                {#<h6 class="my-0">Nicolas Bléhaut</h6>#}
                {#<small class="text-muted">Billet -12 ans</small>#}
            {#</div>#}
            {#<span class="text-muted">15€</span>#}
        {#</li>#}
    </ul>
    <div class="alert alert-warning" role="alert">
        Tarif réduit pour les étudiants, les employés du musée, les membres du service du Ministère de la Culture ainsi que les militaires. Document justificatif à fournir pour l'entrée dans le musée.
    </div>
</div>